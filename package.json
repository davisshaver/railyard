{
  "name": "railyard",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm run build-gutenberg && npm run build-theme",
    "build-gutenberg": "cd public/wp-content/plugins/gutenberg/ && npm install && npm run build",
    "build-theme": "cd public/wp-content/themes/terminal/ && npm install && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^8.0.2",
    "eslint": "^4.11.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-config-airbnb-es5": "^1.2.0",
    "eslint-plugin-googleappsscript": "^1.0.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-react": "^7.5.1"
  }
}
